# Database Directory

Esta carpeta almacena la base de datos SQLite del sistema.

## Archivos Ignorados

Los archivos `.db` no se versionan en Git por seguridad y para evitar conflictos.

## Inicialización

La base de datos se crea automáticamente al ejecutar la aplicación por primera vez.
